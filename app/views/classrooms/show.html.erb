Classroom Info

<%= render partial: 'classroom', locals: {classroom: @sclassroom} %>

W<% @classroom.students.each do |student| %>
  <%= render partial: 'students/student', locals: {student: student} %>
<% end %>
